make和new都用于分配内存

new：很少用，一般用来给基本类型申请内存，string、int等等。返回对应类型的指针。

make：用于内存分配，区别于new，它只用于slice、map和chan的内存创建，而且它返回的类型就是这三种类型本身，而不是它们的指针类型，因为这三种类型就是引用类型，所以就没必要返回它们的指针了。
make函数的签名：func make(t Type,size ...IntegerType) Type



比较切片和数组的区别
 * 1.数组是值类型，将一个数组复制给另一个数组，传递的是一份拷贝
 * 2.切片是引用类型，切片包装的数组称为切片的底层数组


Printf的格式化占位符
%v：任何值类型
%T：查看类型
%d：十进制数
%b：二进制数
%o：八进制数
%x：十六进制数
%c：字符
%s：字符串
%p：指针
%f：浮点数
%t：布尔值


创建指针类型结构体
取结构体的地址实例化

